schema: >
  type Query {f(
    enum: Color!
  ):Int}
  enum Color { red green blue }

template: >
  query {f(
    enum: green,
  )}

inputs:
  Query.f|enum:
    match: true
    tokens:
      - EnumVal: "green"
