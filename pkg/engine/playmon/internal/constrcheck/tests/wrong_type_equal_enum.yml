schema: >
  type Query {f(
    enum: TestEnum!
  ):Int}
  enum TestEnum { something }

template: >
  query {f(
    enum: something,
  )}

inputs:
  Query.f|enum:
    match: false
    tokens:
      - Str: "something" # not an enum value
