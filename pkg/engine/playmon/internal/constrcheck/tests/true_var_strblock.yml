schema: >
  type Query { f(
    a: String!
    b: String!
  ):Int }

template: >
  query { f(
    a = $a: "okay",
    b: $a,
  ) }

inputs:
  Query.f|a:
    match: true
    tokens:
      - StrBlock: "okay"
  Query.f|b:
    match: true
    tokens:
      - StrBlock: "okay"
