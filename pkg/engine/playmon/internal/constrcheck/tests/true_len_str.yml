schema: >
  type Query { f(
    eq:String!
    neq:String!
    gr:String!
    le:String!
    greq:String!
    leeq:String!
  ):Int }

template: >
  query { f(
    eq:   len 0,
    neq:  len != 0,
    gr:   len > 1,
    le:   len < 2,
    greq: len >= 3,
    leeq: len <= 4,
  ) }

inputs:
  Query.f|eq:
    match: true
    tokens:
      - Str: "" # eq
  Query.f|neq:
    match: true
    tokens:
      - Str: "123456789" # not eq
  Query.f|gr:
    match: true
    tokens:
      - Str: "12" # gr
  Query.f|le:
    match: true
    tokens:
      - Str: "1" # le
  Query.f|greq:
    match: true
    tokens:
      - Str: "123" # eq
  Query.f|leeq:
    match: true
    tokens:
      - Str: "1234" # eq
