schema: >
  type Query { f(
    a: Boolean!
    b: Boolean!
  ):Int }

template: >
  query { f(
    a = $a: true,
    b: (true && true) && $a,
  ) }

inputs:
  Query.f|a:
    match: true
    tokens:
      - True:
  Query.f|b:
    match: true
    tokens:
      - True:
