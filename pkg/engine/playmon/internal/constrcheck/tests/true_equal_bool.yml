schema: >
  type Query {f(
    bool_t:   Boolean!
    bool_f:   Boolean!
  ):Int}

template: >
  query {f(
    bool_t:   true,
    bool_f:   false,
  )}

inputs:
  Query.f|bool_t:
    match: true
    tokens:
      - True:
  Query.f|bool_f:
    match: true
    tokens:
      - False:
