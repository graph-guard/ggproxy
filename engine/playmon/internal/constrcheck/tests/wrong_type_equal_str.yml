schema: >
  type Query {f(
    string: String!
  ):Int}
  enum TestEnum { something }

template: >
  query {f(
    string: "something",
  )}

inputs:
  Query.f|string:
    match: false
    tokens:
      - EnumVal: "something"
