schema: >
  type Query {f(
    a: Int!
    b: Int!
    c: Int!
  ):Int}

template: >
  query {f(
    a: >0 && <10,
    b: >0 && <10,
    c: >0 && <10 && != 5,
  )}

inputs:
  Query.f|a:
    match: true
    tokens:
      - Int: 4
  Query.f|b:
    match: true
    tokens:
      - Int: 4
  Query.f|c:
    match: true
    tokens:
      - Int: 4
