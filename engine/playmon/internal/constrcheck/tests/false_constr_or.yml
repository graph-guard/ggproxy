schema: >
  type Query {f(
    a: Int!
    b: Int!
    c: Int!
  ):Int}

template: >
  query {f(
    a: 1 || 0,
    b: 1 || 0,
    c: 1 || 0 || 2,
  )}

inputs:
  Query.f|a:
    match: false
    tokens:
      - Int: 42
  Query.f|b:
    match: false
    tokens:
      - Int: 42
  Query.f|c:
    match: false
    tokens:
      - Int: 42
